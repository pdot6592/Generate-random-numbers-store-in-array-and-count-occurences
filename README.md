# Generate-random-numbers-store-in-array-and-count-occurences
C++: This program generates random numbers between 1 and 6, stores in an array, sorts the array and counts the occurrences of each number
